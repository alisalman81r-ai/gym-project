{
  "name": "ironpulse-fitness",
  "version": "1.0.0",
  "description": "IronPulse Fitness gym website",
  "private": true,
  "scripts": {
    "start": "live-server --port=5500",
    "dev": "live-server --port=5500"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
