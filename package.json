{
  "name": "ironpulse-fitness",
  "version": "1.0.0",
  "description": "IronPulse Fitness gym website",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "start": "npm --prefix web run dev",
    "dev:legacy-static": "live-server --port=5500"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
